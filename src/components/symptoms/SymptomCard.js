//Component builds cards for each symptom added to the symptom list
import React, { Component } from "react"

export default class SymptomCard extends Component {

    render() {
        return (
            <>
            {console.log("symptom card:", this.props)}
            </>
        )
    }
}